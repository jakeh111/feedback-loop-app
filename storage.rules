rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // A user can read and write to their own directory
    match /{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
